# DOLFINx_HDG

## Installation

### C++
To build and install, from the ``cpp/`` directory, run:
```
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

### Python
To build and install, from the ``python/`` directory, run:
```
pip3 install .
```
