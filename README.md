# DOLFINx_HDG

## Installation

### C++
To install, from the ``cpp/`` directory, run:
```
mkdir build
cd build
cmake ..
cmake --install .
```

### Python
To build, from the ``python/`` directory, run:
```
mkdir build
cd build
cmake ..
cmake --build .
```
